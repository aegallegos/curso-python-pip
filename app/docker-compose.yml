services:
  app-csv:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app-csv
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ENV=development